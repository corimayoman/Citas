[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && (npx ts-node prisma/seed.ts || true) && node dist/main.js"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
